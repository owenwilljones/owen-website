{
  "name": "owen-website",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy",
    "prettier": "npx prettier . --write",
    "link-check": "npm run build && npx linkinator _site --recurse"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@11ty/eleventy-plugin-rss": "^2.0.4",
    "linkinator": "^7.5.0",
    "prettier": "^3.6.2",
    "sass": "^1.93.3"
  }
}
